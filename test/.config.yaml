server:
  port: 8080

  ssl_port: 4343
  ssl_certificate: self-signed.crt
  ssl_certificate_key: self-signed.key

  # post_max_size: 1000

  # keep_alive_timeout: 0

# logger:
#   info: data/info.log
#   error: data/error.log

database:
  # hostname: localhost
  # port: 27017

  name: test

  user: user
  password: abc123
  # authentication_database: test

  # pool_size: 1

  # ignore_undefined: true

# key:
  # size: 64
  # encoding: base64

session:
  secret: session-cookie-secret

  # resave: false
  # save_uninitialized: false

  # cookie:
  #   secure: false
  #   signed: true
  #   path: '/'
  #   http_only: true
  #   same_site: 'strict'

# project-specific configurations

misc:
  name: value
